<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @worker.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @worker.description %>
</p>

<p>
	<strong>Skills:</strong>
	<%= render @worker.skills %>
	<% if @worker.user == current_user %>
	<div>
		<%= render "skills/form"%>
	</div>
	<% end %>
</p>



<% if @worker.user == current_user %>
<%= link_to 'Edit', edit_worker_path(@worker) %> |
<%= link_to 'Back', workers_path %>
<% end %>